

conky.config = {

out_to_x = false,
out_to_console = true,
background = false,
double_buffer = false,

update_interval = 1,
total_run_times = 0,
override_utf8_locale = false,
short_units = true,
if_up_strictness = 'address',
cpu_avg_samples = 2
}

conky.text = [[
 CPU:${cpu cpu0}%    RAM:${mem}    FILE:${fs_used /}    ${time %Y/%m/%d   %a}    ${time %H:%M}    ${battery_percent}%
]]
